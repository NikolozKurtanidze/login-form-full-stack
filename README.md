# login-form-full-stack
  Simple login form full-stack react app which consumes spring RESTapi to get users list(I've used mysql database). 
Checking of username and password is performed on the server-side.
